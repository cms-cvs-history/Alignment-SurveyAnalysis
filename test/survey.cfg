process TEST =
{
  include "FWCore/MessageService/data/MessageLogger.cfi"
  include "Alignment/SurveyAnalysis/test/detector.cfi"

  source = EmptySource {}

  untracked PSet maxEvents = { untracked int32 input = 0 }

  module survey = SurveyTest
  {
    untracked bool bias = false
    uint32 iterator = 100
    string fileName = "survey/shifts.root"
    string algorith = "points"
    vstring hierarch = {"Petal"}
  }

  path p = { input, survey }
}
